<h1>Users#index</h1>
<% @users.each do |user| %>
<%= user.email %>
<% if user.avatar.attached? %>
<div class="max-w-64"><%= image_tag rails_blob_url(user.avatar, disposition: "inline") %></div>
<% else %>
<p>Avatar não disponível.</p>
<% end %>
<% end %>
